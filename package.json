{
  "name": "smart-city-surveillance",
  "version": "1.0.0",
  "description": "Smart City AI Surveillance System with real-time monitoring and AI threat detection",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && uvicorn app:app --host 0.0.0.0 --port 8000 --reload",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && pip install -r requirements.txt && cd ../frontend && npm install",
    "start": "npm run dev"
  },
  "keywords": ["smart-city", "surveillance", "ai", "computer-vision", "fastapi", "react"],
  "author": "Smart City Development Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.13.6"
  }
}
